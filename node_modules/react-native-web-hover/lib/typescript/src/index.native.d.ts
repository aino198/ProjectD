import * as React from 'react';
import { Pressable, ScrollView, FlatList, VirtualizedList, ViewProps } from 'react-native';
interface HoverableProps extends ViewProps {
    children: React.ReactNode | ((state: {
        hovered: boolean;
    }) => React.ReactNode);
}
export declare function Hoverable({ children, ...rest }: HoverableProps): JSX.Element;
export { Pressable, ScrollView, FlatList, VirtualizedList };
